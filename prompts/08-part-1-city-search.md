# 08 — Part 1 City Search Requirements

Purpose: Preserve the reviewed assignment requirements for the first connected hotel-search milestone.

## Requirements

- Provide one city-search input and a Search button.
- Show matching hotel stays in a plain table with clear column labels.
- Show a clear message when no results match.
- Read `hotels.csv` and `trips.csv` in Python and join their records through `hotel_id`.
- Return matching joined records from FastAPI to Vue.
- Check a successful city search and a no-results search in the browser.
- Record expected and observed results before the Git checkpoint.
- Have the user manually scan the changes before committing and pushing the reviewed Part 1 milestone.
